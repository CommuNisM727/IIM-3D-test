
import numpy as np

def solve():
    """
    Args:
        n_x (integer): number of points along x-axis.
        n_y (integer): number of points along y-axis.
        x_inf (double): lower bound of x.
        x_sup (double): upper bound of x.
        y_inf (double): lower bound of y.
        y_sup (double): upper bound of y. (ASSUME TO BE SQUARE AREA)
        
    Returns:
    """

solve()

